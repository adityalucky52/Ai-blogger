config:
  target: "http://localhost:5000"
  phases:
    - duration: 10
      arrivalRate: 100
      name: "Sustained load"
scenarios:
  - name: "Fetch all blogs"
    flow:
      - get:
          url: "/api/blogs"
